name: Paper Artifact

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'
  pull_request:
    paths:
      - 'Cargo.lock'
      - 'Cargo.toml'
      - 'docs/**'
      - 'examples/paper_vectors.rs'
      - 'formal/**'
      - 'scripts/audit-paper-references.sh'
      - 'scripts/reproduce-paper-artifact.sh'
      - 'src/**'
      - 'tests/paper_vectors.rs'
      - '.github/workflows/paper-artifact.yml'

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  paper-artifact:
    name: Paper artifact bundle
    runs-on: ubuntu-latest
    timeout-minutes: 75
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            cm-super \
            latexmk \
            lmodern \
            protobuf-compiler \
            texlive-fonts-recommended \
            texlive-lang-cyrillic \
            texlive-latex-base \
            texlive-latex-extra \
            texlive-publishers \
            texlive-science

      - uses: dtolnay/rust-toolchain@d1031067263f94b142dd6c0ce24c5eb9d02d52a0 # stable
        with:
          toolchain: "1.96.1"

      - uses: Swatinem/rust-cache@23bce251a8cd2ffc3c1075eaa2367cf899916d84 # v2.7.3

      - name: Quick artifact checks
        run: ./scripts/reproduce-paper-artifact.sh quick

      - name: Build paper PDFs
        run: ./scripts/reproduce-paper-artifact.sh paper

      - name: Audit paper references
        run: ./scripts/reproduce-paper-artifact.sh references

      - name: Upload artifact-output bundle
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: aura-paper-artifact-${{ github.sha }}
          path: artifact-output/**
          if-no-files-found: error
          retention-days: 90
